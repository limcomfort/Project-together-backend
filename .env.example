PORT=5000
# Подключение к MongoDB. Если не указано — используется mongodb://127.0.0.1:27017/together
# При недоступном сервере в режиме разработки автоматически поднимается in-memory MongoDB
MONGODB_URI=mongodb://127.0.0.1:27017/together
